# GraphQL Character Fetcher

This project contains GraphQL queries to retrieve specific character information by ID using the Rick and Morty API.

## Usage
Each `.graphql` file contains a query for a specific character ID (1, 2, 3, 4).
The corresponding `.json` file contains the expected output from the API.

## Endpoint
https://rickandmortyapi.com/graphql